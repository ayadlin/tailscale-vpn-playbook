# Docker Compose environment variables (copy to docker/.env or export in shell)
# Required
TS_AUTHKEY=tskey-xxxxxxxxxxxxxxxxxxxxxxxx

# Optional
TS_HOSTNAME=docker-exit-node

# Optional: extra flags passed to tailscaled (rarely needed)
TS_EXTRA_ARGS=
